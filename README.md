This program creates a dynamic array of 100 items and gives each one a name and ID number. The user enters an ID to search for, and the program uses a binary search function to quickly
find the matching item. If the item exists, it prints the details; if not, it tells the user it wasn't found.
